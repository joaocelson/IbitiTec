@model IEnumerable<LDApp.Models.Comerciante>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NomeFoto)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NomeFoto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ComercianteId }) |
            @Html.ActionLink("Details", "Details", new { id = item.ComercianteId }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.ComercianteId })
        </td>
    </tr>
}

</table>
